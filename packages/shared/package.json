{
  "name": "@dreamcacher/shared",
  "version": "0.1.0",
  "main": "./dist/index.js",
  "typings": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch  --preserveWatchOutput",
    "test": "yarn build && export APP_ENV=test && jest \"dist/.*\\.test\\.js\""
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^4.1.2"
  }
}
